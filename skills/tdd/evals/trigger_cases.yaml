positive:
  - write the failing regression test first, then make it pass
  - TDD this behaviour one vertical slice at a time
  - use red green refactor through the public interface
  - add the first failing test before touching production code
negative:
  - implement this approved specification through review and final acceptance
  - diagnose why this existing test fails without changing source
  - review this pull request without implementing fixes
  - help me decide the feature requirements
boundary:
  - write only the first failing test and stop before production code
  - use TDD as one slice inside the wider implement run
  - reproduce this bug as a test before fixing it
