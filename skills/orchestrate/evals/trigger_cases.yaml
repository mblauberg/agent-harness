schema_version: 1
target_skill: orchestrate
cases:
  - {id: q154, relation: positive, prompt: "Fan out this decomposable repository-wide audit across bounded read-only agents with independently checkable source-anchored returns, then synthesise it.", tags: [direct], expected: {primary_skill: orchestrate, companion_skills: []}}
  - {id: q155, relation: positive, prompt: "Use Herdr to split a new pane and run the test suite there.", tags: [direct, herdr-control], expected: {primary_skill: orchestrate, companion_skills: []}}
  - {id: q730, relation: positive, prompt: "Autonomously implement the accepted/ready issue queue: pull each ready/accepted issue and run it through implement to a PR, stopping at the human review gate for every issue.", tags: [direct, autonomous-implementation], expected: {primary_skill: orchestrate, companion_skills: []}}
  - {id: q157, relation: negative, prompt: "This README mentions Herdr; fix its one-character typo.", tags: [no-skill, passive-mention], expected: {primary_skill: null, companion_skills: []}}
  - {id: q158, relation: negative, prompt: "These tightly coupled symptoms share one unknown error source and mutable fixture; reproduce the root cause serially without editing.", tags: [adjacent], expected: {primary_skill: diagnose, companion_skills: []}}
  - {id: q159, relation: negative, prompt: "Run this open-ended research mission across crashes until I explicitly write STOP.", tags: [adjacent], expected: {primary_skill: autopilot, companion_skills: []}}
  - {id: q160, relation: boundary, prompt: "Implement this approved migration in partitioned agent lanes; stop each lane ready-to-merge, then have one chair integrate serially from current main and rerun checks and reviews after any tree change.", tags: [composition, serial-integration], expected: {primary_skill: implement, companion_skills: [orchestrate]}}
  - {id: q161, relation: boundary, prompt: "Start a Claude pane for an answer-bearing read-only PR review; send and collect the review through Fabric, using Herdr only to wake the pane.", tags: [composition, herdr-control], expected: {primary_skill: code-review, companion_skills: [orchestrate]}}
  - {id: q162, relation: boundary, prompt: "Operate this persistent agent lab using bounded orchestration waves until human STOP.", tags: [composition], expected: {primary_skill: autopilot, companion_skills: [orchestrate]}}
