interface:
  display_name: "agy Headless (Gemini)"
  short_description: "Best-effort sandboxed Gemini-family scouting via the agy (Antigravity) CLI"
  default_prompt: "Use $agy-headless to preflight agy, run a bounded sandboxed Gemini-family scout pass, and return verified findings."
policy:
  allow_implicit_invocation: false
