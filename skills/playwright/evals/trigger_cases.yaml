schema_version: 2
target_skill: playwright
cases:
  - {id: q163, relation: positive, prompt: "Drive the real browser from the terminal, snapshot the page, and capture the checkout result.", tags: [direct], expected: {primary_skill: playwright, companion_skills: []}}
  - {id: q164, relation: positive, prompt: "Navigate this site with playwright-cli and extract the visible table data.", tags: [direct], expected: {primary_skill: playwright, companion_skills: []}}
  - {id: q165, relation: positive, prompt: "Open the running app, follow the modal flow using fresh element refs, and take screenshots.", tags: [direct], expected: {primary_skill: playwright, companion_skills: []}}
  - {id: q166, relation: negative, prompt: "Write a maintained Playwright end-to-end test for this new user behaviour.", tags: [adjacent], expected: {primary_skill: tdd, companion_skills: [implement]}}
  - {id: q167, relation: negative, prompt: "Redesign the landing page and implement its new visual hierarchy.", tags: [adjacent], expected: {primary_skill: frontend-design, companion_skills: []}}
  - {id: q168, relation: negative, prompt: "Fetch this public JSON endpoint once and show the response.", tags: [no-skill], expected: {primary_skill: null, companion_skills: []}}
  - {id: q169, relation: boundary, prompt: "Use a real browser to reproduce this UI failure and isolate its root cause without fixing it.", tags: [composition], expected: {primary_skill: diagnose, companion_skills: [playwright]}}
  - {id: q170, relation: boundary, prompt: "Review the running interface at narrow and wide widths in a real browser; make no source edits.", tags: [composition], expected: {primary_skill: frontend-review, companion_skills: [playwright]}}
  - {id: q171, relation: boundary, prompt: "Implement the approved checkout fix and verify the user flow in a real browser before review.", tags: [composition], expected: {primary_skill: implement, companion_skills: [playwright]}}
