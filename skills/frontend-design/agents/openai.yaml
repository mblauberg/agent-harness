# Modified from Impeccable for this harness; see ../NOTICE.md.
interface:
  display_name: Frontend Design
  short_description: Design, implement, redesign, or polish frontend interfaces
  default_prompt: Use $frontend-design alone for bounded non-source design work; use $implement with $frontend-design for source changes, and $frontend-review for read-only critique.
