schema_version: 1
target_skill: github-setup
cases:
  - {id: q900, relation: positive, prompt: "Set up GitHub for this project: labels, branch ruleset, issue forms, PR template, and a Project board.", tags: [direct], expected: {primary_skill: github-setup, companion_skills: []}}
  - {id: q901, relation: positive, prompt: "Scaffold a fresh repository with declarative labels and a main-branch ruleset pinned to a single required check.", tags: [direct], expected: {primary_skill: github-setup, companion_skills: []}}
  - {id: q902, relation: positive, prompt: "This new repo has no issue forms, PR template, or work-item runbook yet; bootstrap them from the proven pattern.", tags: [direct], expected: {primary_skill: github-setup, companion_skills: []}}
  - {id: q903, relation: negative, prompt: "Walk this accepted issue from Ready through merge using this repository's own GitHub work-item runbook.", tags: [adjacent], expected: {primary_skill: null, companion_skills: []}}
  - {id: q904, relation: negative, prompt: "Implement the approved change on its feature branch and open the pull request per our existing template.", tags: [adjacent], expected: {primary_skill: implement, companion_skills: []}}
  - {id: q905, relation: negative, prompt: "Draft a spec proposing new acceptance criteria for the release gate; no repository scaffolding is involved.", tags: [adjacent], expected: {primary_skill: scope, companion_skills: []}}
  - {id: q906, relation: boundary, prompt: "Author a global skill capturing our GitHub bootstrap pattern so other projects can reuse it.", tags: [composition], expected: {primary_skill: github-setup, companion_skills: []}}
  - {id: q907, relation: boundary, prompt: "This repo already has labels and a ruleset; just merge today's accepted pull request per its runbook.", tags: [composition], expected: {primary_skill: null, companion_skills: []}}
  - {id: q908, relation: boundary, prompt: "Set up GitHub for this project, then implement the first accepted issue once the board exists.", tags: [composition], expected: {primary_skill: github-setup, companion_skills: [implement]}}
