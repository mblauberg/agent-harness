Superseded by the epic #124 skill-catalogue restructure.

The #124 renames (autonomous-lab -> autopilot, frontend-design/frontend-review
-> ui-ux-design, skill-audit/skill-authoring -> skill-craft) and the reworded
descriptions invalidate this frozen routing bundle: catalogue.txt, the routing
packet, the plan digests and the real Agent-Fabric provider answers in
routing-result.json are bound together by SHA-256 and no longer match the live
catalogue.

The evidence was NOT hand-edited. Fabricating evaluation evidence is forbidden,
and routing-result.json carries genuine blind-provider routing answers. Honest
re-greening needs a live Agent-Fabric routing run against the new catalogue.

Tracked in: #135 (spec05 retirement + eval regeneration). That issue owns
re-running the evaluation, refreshing this bundle and deleting this marker.
Until then scripts/check-harness waives the three direct Spec 05 gates
(validate-inputs, probe, validate-routing); the equivalent pytest tests are
xfail(strict=False) for the same reason. On main the marker is absent and the
gates enforce fully.
