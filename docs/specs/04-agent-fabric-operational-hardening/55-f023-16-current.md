Invalid paths or digest-identity collisions remain explicitly quarantined and
unprojected rather than crashing a codec or guessing provenance. Every receipt
and intake binding references an exact registry ID. Each intake binding has a
foreign key and trigger proving its repeated path/digest equals one active
same-scope registry row.
