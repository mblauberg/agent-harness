
Acceptance additionally requires deterministic oracles for:

- current-baseline integrity and atomic-install rollback, every composite
  foreign key, immutable draft/binding/resolution content, contiguous grant
  revisions, one-active-revision, exclusive grant/gate choice, every four-
  owner state combination, one active reservation per common directory and
  indexed current-grant/draft/recovery queries;
