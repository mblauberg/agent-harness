
## 9. Project Console daemon and persistence

Spec 01 owns the protocol entities and atomic coordination invariants. This
spec owns their current persistence, lock-safe on-demand daemon bootstrap,
global liveness and stop predicates, notification outbox and crash recovery.
