
- Spec 01 owns coordination, authority, provider-session, project-session,
  operator-principal, scoped-gate, result-delivery and atomic request/reply/
  task-completion requirements.
- Spec 02 owns the adaptive harness and delivery lifecycle.
- Spec 03 owns model-adapter activation and Herdr observation.
- Spec 04 owns protocol, trust, operational hardening, lock-safe bootstrap,
  global daemon liveness/stop, the current persistence baseline and crash
  recovery.
- This spec owns the project Console, operator projection, adaptive session
  launch, human-attention workflow, Herdr control integration and optional
  Git/GitHub operator adapters.

Specs 01 and 04 are the accepted canonical owners for those requirements.
Product requirements remain here; transaction, schema and daemon invariants
remain with their canonical owners.
