# Decision register

Statuses below reflect the 2026-07-13 scoping session (`SCOPING-SESSION.md`):
findings verified against HEAD `babd47a`, codex-pair challenge
(`challenges/codex-pair-round1.md`), and human decisions via /scope + /grill-me.
Framing decisions for the session: **personal-first, product-compatible**
(round 1); **headless managed sessions may write via authority profiles**
(round 2); **codex 4-step staged path now, foundations in parallel** (round 3).

| ID | Recommended decision | Outcome (2026-07-13) | Notes |
|---|---|---|---|
| D-001 | Use a modular monolith with one SQLite authority | **Accepted** | Uncontested by both reviewers. |
| D-002 | Provider-native APIs own session mechanics | **Accepted** | Already the live direction. |
| D-003 | Fabric owns neutral authority, work, evidence and reconciliation | **Accepted** | Core differentiator confirmed. |
| D-004 | Add capability/authority profiles | **Accepted with modifications** (round 2) | Codex mods: only `review-readonly` + `workspace-write-offline` initially; profile = requested bundle, effective = monotone intersection with human envelope; receipts bound to authority digest + compiler version; authority-schema reconciliation is a prerequisite. |
| D-005 | Separate workspace writes from external effects | **Accepted with modifications** | Extend existing `ExternalEffectService` custody model; logical credential boundary now, separate process only if threat model demands. |
| D-006 | Add one generated harness manifest | **Rejected as written** (round 4) | Replaced by per-domain owners: each domain's real source generates/validates its projections, CI drift checks per domain; at most a derived read-only index. |
| D-007 | Make lifecycle an executable policy | **Accepted with modifications** | Extend the existing delivery kernel (`delivery-run` v1, `validate_delivery.py`) and project into Fabric; executable minima only (risk floor, gates, repair bounds, retention class); judgement calls stay with chair/skills; no second policy model. |
| D-008 | Add `architecture-review`; keep `refactor` implementation-focused | **Accepted** (round 7) | Promotion approved via the normal skill-authoring path; implementation pending. |
| D-009 | Implement intake, execution plans and backlog as schemas/runtime | **Partially accepted** (round 5) | Backlog: schema-first, store-pluggable (repo markdown frontmatter OR GitHub Issues per project convention, lossless migration). Runtime queue controller and intake kernel deferred until the write pilot proves out; F-009/F-010 demoted from P0. |
| D-010 | Default paired Claude/Codex scoping for broad consequential work | **Accepted** | This session is the operating pattern. |
| D-011 | Use risk/oracle-adjusted certifying review | **Accepted; amendments pending** (round 7) | Other-primary review stays mandatory for crucial+ and load-bearing decisions; below that, derive pressure from risk + oracle strength. Requires HARNESS.md and Spec 05 amendments before activation. |
| D-012 | Use MCP for focused tools/context, not whole orchestration | **Accepted** | Uncontested. |
| D-013 | Keep Herdr optional and non-authoritative | **Accepted** | Current boundary correct. |
| D-014 | Add governed retention deletion | **Accepted, resequenced** (round 6) | Design + 5-class taxonomy now; class-tag new state immediately; delete machinery after tranche 1; archive-only meanwhile. F-008 demoted from P0. |
| D-015 | Split portable configuration from local attestations | **Accepted, scope corrected** | Verification: defect is entirely in `config/adapter-compatibility.yaml`; `config/agent-fabric.yaml` is already clean. |
| D-016 | Use one root workspace before advanced build tooling | **Accepted — foundations leg** | At the scoping baseline, Fabric CLI failed with `ERR_MODULE_NOT_FOUND`; Lane B owns the active root-workspace repair. |
| D-017 | Console core depends on protocol only | **Accepted** | Engineering call, uncontested. |
| D-018 | Direct cutover by default before stable release | **Accepted** | Both reviewers agree; pairs with F-020 compat-waiver policy. |
| D-019 | Add explicit local threat modes | **Accepted, strengthened** | Codex round 1: isolation-substrate attestation (worktrees are NOT permission boundaries) becomes a hard gate on the write pilot, not just documentation. |
| D-020 | Proposal-first, evaluated self-improvement | **Accepted, narrowed** | Manual flywheel already exists in `skills/retrospect`; runtime enforcement deferred (not P0). |
| D-021 | Autonomous LLM-resolved implementation; PR review the only human gate | **Accepted — human directive** (2026-07-13) | codex `gpt-5.6-sol` chair (xhigh/max/ultra) + Opus pair implement every lane; each decision is LLM-resolved (chair discretion or council vote) and recorded here. Former human gates (spec-amendment acceptance, containment-spike verdict, 80×24 usability, final acceptance) become council decisions, landing via PR. Preserved boundaries (not delegated): no push-to-`main`/release/deploy/credential mutation, no network egress or external-effect enablement; the write-profile containment spike is still executed adversarially (only its verdict is adjudicated); `.agent-run/AFAB-004` never accessed. Charter: [`CHAIR-CHARTER.md`](CHAIR-CHARTER.md). |
| D-022 | Close CAPA-001 lifecycle receipt/schema choices Q1-Q5 | **Accepted with executable corrections — council** (2026-07-14) | Q1 uses one authenticated `fresh-origin` subject/effect/replay with exactly seven batch arms and a crash-safe scope-admission outbox; preparation order is handoff → batch → effect → intents. Q2 derives issue status from immutable issue facts plus one canonical source head, uses `BEGIN IMMEDIATE`, reciprocal handoff/revocation guards and no copied clocks. Q3 uses canonical scope/loss heads. Q4 uses exact compare-delete under `BEGIN IMMEDIATE`, without re-keying. Q5 assigns generic route recovery to a distinct `GenericProviderRouteRecoveryService`. The chair adopted the majority design after SQLite oracles exposed and corrected immediate-FK ordering and nullable-CHECK escapes; one native adversarial dissent on the pure-fresh authority shape is retained in the CAPA-001 council receipt. |
| D-023 | Freeze the capability-compiled write-profile amendment before the family split | **Accepted with mandatory safety deltas — council** (2026-07-14) | Direct `AuthorityEnvelopeV2` cutover; exactly `review-readonly` and `workspace-write-offline`; monotone compilation from human envelope, task/worktree ownership, risk policy, provider capability and local attestation; admitted/rejected compiler/native-settings receipts; certifying work stays read-only. `workspace-write-offline` is generic-only, one-owned-worktree, no tool egress/secrets/deployment/irreversible action/external effect and remains inert until the exact Step-3 provider tuple passes containment. Unmet predicates fail before provider I/O; there is no implicit downgrade. Opus initially proposed transparent downgrade, then withdrew it after replay/side-effect analysis; the independent native voter and chair concurred. Spec 03's former open write-profile hook is subordinated to Spec 01. Agent Fabric bonus-family seats were unavailable (`status`: all project seats inactive/unregistered), so the degraded council used Codex chair/native safety lens plus the standing Opus other-primary and objective pre-I/O/replay evidence. |
| D-024 | Package frozen Specs 01, 04 and 05 as bounded reversible families | **Accepted with structural controls; F-023 partial — D-021 chair owner-call after mandatory review** (2026-07-14) | The default binding loader reconstructs the exact frozen bytes at `0305376624fdb03e14166a2a831e0053fca367c9`; hashes bind module roles/order, source ranges, link relocation, standalone-fence scaffolding and both binding/candidate sequences. Spec 04's long SQL fence closes/reopens only after complete top-level statements, and every physical module is independently fence-balanced. The filtered topical sequence is explicitly **non-binding**: live supersession preambles sit outside it, while it still contains narrow obsolete/mixed clauses (Spec 01 legacy-import/migration and v0.36-baseline wording; Spec 04 additive migrations 0010/0013, legacy import/backfill, old-client and compatibility-decoder wording; Spec 05 a duplicate version chronology and stale pending-freeze wording). F-023 therefore remains partial/open. W017 must derive net-current prose through hash-bound supersession mapping and net-effective-equivalence proof; the earlier 72%/93% amendment-history estimate overclassified cumulative current requirements. No version is bumped by this structural package. Under D-021, the chair explicitly authorises the required `scripts/check-harness` family gate and `tests/test_provider_skill_boundaries.py` verified-loader migration; neither change widens runtime authority. |
| D-026 | Keep exact delivery receipts private while making release evidence portable and independently auditable | **Accepted with pre-first-push safeguards — degraded council** (2026-07-14) | Exact `.agent-run/<id>/` receipts remain local, ignored and validator-readable; they are never force-tracked. Remove the already-tracked CAPA-001/W007 entries from the index without deleting local bytes, project durable verdicts/digests/tested-tree facts into tracked governance and the PR body, remove bootstrap absolute-home substitution, and add zero-tracked-run plus current-tree/history regressions without weakening the release checker. Add a fail-closed publication-range mode because `--history` traverses unrelated local refs: it validates the selected HEAD tree independently of the checkout, rejects forbidden paths in every selected commit, and scans selected tree content, commit messages and author emails. At W014 only—after the entire accepted tree is clean—rebuild the still-unpushed `comprehensive-review` ref as one public-safe commit parented to then-current `origin/main`, using the identical accepted tree and an old-value compare-and-swap. Keep recovery in the existing ref's reflog plus the private programme receipt, not a second branch/tag; rerun and rebind all final deterministic, security, native, Opus and hosted gates to the new SHA before one exact non-force first push. Pushing the tainted PR head and relying on a later squash is prohibited. Standing Opus vote/addendum and a fresh native audit concurred; bonus-family seats were unavailable, so objective tree/graph/push-set proofs are mandatory. This changes no runtime/spec authority or receipt schema. |
| D-027 | Extend W017 Rule 5 consolidation to the complete 14-candidate binding-current set | **Accepted — degraded council; second/final repair authorised** (2026-07-14) | The exhaustive native audit (`fd591dd9…`) and full-set Opus adjudication (`71fde3ab…`) independently classify all R5-01..R5-14 as positive amendment, migration, forward-repair or version-chronology mandates that require explicit supersession ownership. No candidate is rejected. W017 must preserve every net-current obligation while removing historical/future procedure: fold R5-03 into expanded F023-04, give the other 13 candidates durable map/current/archive ownership, lock R5-06 with R5-08, encode explicit mixed-obligation matrices for R5-06/R5-11/R5-14, preserve R5-13's exact nullability split, and add a systemic oracle rejecting any newly unclassified positive shared-current mandate. Version/status remain unchanged unless exact equivalence proof forces a bump. This is repair cycle 2/2; there is no third discovery cycle. Bonus-family seats remain unavailable, so exhaustive native and Opus evidence plus fresh final exact-commit reviews are mandatory. |
| D-028 | Bind W014 publication evidence to the raw, unvirtualized Git object graph | **Accepted — degraded council; W018 final repair authorised** (2026-07-14) | Native re-audit (`45dfcf4f…`) and Opus adjudication (`53f30627…`) independently reproduced a false CLEAN proof: default/custom replacement refs, `.git/info/grafts` and inherited shallow/environment overrides can make the checker inspect a clean virtual graph while a push publishes tainted raw commits. One hardened Git endpoint must serve publication-range and history proof: disable replacements in every namespace; sanitize inherited repository/config/object redirection while preserving content-addressed repository alternates; neutralize and explicitly reject graft/shallow views; enumerate ancestry and the selected set from raw `cat-file commit` parent headers; and bind the raw endpoint SHA, HEAD tree, every selected path history, full messages and authors. Default working-tree mode remains unchanged. Regressions must independently reopen under removal of each control and include a clean linked-worktree/alternate case. This is W018 repair cycle 2/2; no publication commit is authorised until an independent re-audit is CLEAN. |
| D-029 | Recover W005 right-reason TDD through a narrow pair-classifier cut before coordinator admission | **Accepted — degraded council; serial W005-C/W005-B order** (2026-07-14) | The designated preflight tracer initially had two causes: the intended absent `resolving` row and an earlier `SQLITE_ERROR` because `#assertGenericProviderAction` queried nonexistent `lifecycle_rotation_custody.action_id`. The latter is a narrow W005-C action-ID-only pair-classifier defect, not the broader lifecycle-model cut. First add a single-cause current-schema/classifier oracle; then move outer command replay first and key every generic/special custody read by exact `(adapter_id, action_id)` using existing current columns, with a same-action/different-adapter regression and no lifecycle-state/DDL compatibility change. Rerun the held tracer and require the absent preflight as its sole failure with zero unhandled errors; only then implement the coordinator to GREEN. Integrate C before a separate serial W005-B lifecycle direct-cut subunit. The fixture subowner cannot edit production, but W005 is the whole Lane D build artefact and may contain both serial production subunits. Native artifact full SHA-256 `db0b2eb9…` (self-bound prefix `e2190fdc…`) and corrected Opus artifact `286a05a3…` independently concur. Bonus-family seats remain unavailable. |
| D-030 | Correct all demonstrated W018 post-cap publication-proof false cleans and boundedness escapes before commit | **Accepted — degraded council; exact four-defect correction** (2026-07-14) | Both mandatory exact-stage reviewers returned REVISE on frozen patch `8e45e297…`. Native artifact `89956666…` reproduced a P1 false clean for secret-bearing direct blob refs because streamed `raw_ref_blob_ids` findings are discarded, plus P2 unbounded raw-tree name retention and temp-file stderr storage (`self.names` and the backing file grow while accounting/display caps still report bounded operation). Opus artifact `f7c3d226…` independently reproduced a fourth P1: an end-of-blob OpenAI-key run with an embedded `sk-`/`sk-ant-` restart can pass streaming range/history while default whole-content scanning rejects the same tree. Chair accepts every demonstrated counterexample despite the reviewers' disagreement on the other reviewer's surface. One bounded correction may change only `scripts/public_release_check.py`, `tests/test_public_release_check.py` and private W018 evidence: include direct/tag-to-blob findings with SHA-1/SHA-256 regressions; impose a portable bounded tree-component rule and streaming canonical-order/duplicate validation with measured retention; replace unrestricted child-stderr storage with a concurrently drained fixed-cap buffer joined on normal/timeout/terminate/kill paths; and make streaming content verdict-equivalent under embedded-prefix/end/chunk partitions, including deterministic property pressure. Each defect requires a right-reason RED, focused plus combined gates, a new exact stage and fresh native/Opus changed-surface review. No commit, integration, broader discovery or release authority. Bonus-family seats remain unavailable. |
| D-031 | Replace W005's elapsed-time preflight tracer with an exact capability-entry observation boundary | **Accepted — chair adopts stronger native finding over Opus CLEAN** (2026-07-14) | Native precommit artifact `fd30ef0c…` shows the 100 ms SQLite sample cannot certify preflight-before-capability inspection: a capability-first implementation may insert while the fake response is delayed and pass, while scheduling can false-fail a correct implementation. Opus artifact `1bcede8e…` correctly certifies observation safety, current RED transcripts and digest/payload/principal shapes, but its CLEAN timing conclusion is limited to today's no-coordinator state and therefore does not refute that future-GREEN counterexample. Add exactly one inert existing-fault-channel observation point in `src/core/fabric.ts` immediately before task-bound capability inspection; the acceptance test's synchronous fault callback must snapshot the matching preflight and zero dependants through a second connection at that boundary. Remove the 100 ms sample as an oracle. Until the classifier is GREEN the tracer may honestly stop as boundary-not-reached and remains NON-COUNTABLE; immediately afterward it must reach the boundary and fail only on the missing `resolving` row. A mutation moving admission after the hook must kill the test. Scope is the existing acceptance test, this single no-op-without-fault production hook and private W005 evidence/RUN; no classifier/coordinator/schema/helper/provider effect, commit or integration. Fresh native and Opus exact-stage review is mandatory. The Opus review's disclosed offline create-then-remove of ignored worktree `node_modules` had zero network/tracked/index/hash effect but is retained as a process deviation. |
| D-032 | Close the three exact W018 changed-surface gaps exposed by contained D-030 certification | **Accepted — degraded council; exact lifecycle/scanner repair only** (2026-07-14) | Capability-contained Opus artifact `311490d6…` and chair RED evidence `26ad0292…` concur on the frozen `af111eb5…` stage: P1 streaming/whole-regex divergence when a word-prefixed invalid dash-class token greedily bridges a later valid OpenAI/Anthropic restart at a chunk boundary; P2 drainer thread-start failure that increments accounting before start and leaks the already-created child/pipes because `__enter__` never reaches `__exit__`; and P2 silent loss of non-OS worker exceptions. Corrections 1 and 2 are certified; correction 3's normal/exception/deadline/terminate/kill/reap joins and 64 KiB cap are certified. One final bounded repair may change only `scripts/public_release_check.py`, `tests/test_public_release_check.py` and private W018 receipts: mirror the existing terminal-boundary check in the direct-prefix branch; make drainer startup accounting and pre-body child teardown exact; propagate unexpected worker failures at join; and add right-reason regressions for both token families plus both lifecycle edges. No other W018 discovery, source, commit, integration or release authority is granted. Native certification is platform-content-blocked; per direct human instruction the security leg uses a fresh contained Opus author, a separate fresh contained Opus reviewer and chair deterministic replay, with no prompt retries against the native block. |
| D-033 | Take a public-safe early programme checkpoint after W018 and current-main reconciliation | **Accepted — direct human authority; programme remains RUN** (2026-07-14) | The human directs commit, integration and branch push at the next clean boundary, with an exact fresh-session remainder. This specialises D-026's former W014-only timing without weakening its publication controls: after W018 CLEAN and `origin/main@1ddfe24` reconciliation, build one public-safe commit with the exact accepted checkpoint tree and `origin/main` as its sole parent; use synthetic `.invalid` identity, old-value compare-and-swap, no backup ref/tag, and a non-force first push of `comprehensive-review` only. Re-run deterministic, public-range and static-security gates and obtain fresh contained Opus security/merge review on the final commit; native cybersecurity review is not retried after platform blocks. The checkpoint is not W014 acceptance: GOAL stays RUN, W005-W014 and hosted W007 evidence remain open, no PR is opened or merged, and `main` is never pushed. The active review directory remains load-bearing through W012/W013; prune only proven run-owned W018 containment snapshots now, then migrate durable decisions/evidence before terminal directory removal. |
| D-034 | Scan raw committer identity wherever publication proof scans author identity | **Accepted — blocking P1 from D-033 Opus review; exact two-file repair** (2026-07-14) | Fresh contained Opus artifact `903a89a2…` proved a structural false clean: batch and streaming raw-commit parsers validated but discarded the committer email, while both range/history personal-email checks saw authors only. A clean synthetic author plus personal committer therefore passed; the existing test set both identities together and could not detect the gap. One bounded Opus-authored TDD repair changes only `scripts/public_release_check.py`, `tests/test_public_release_check.py` and private checkpoint evidence: require/retain `committer_email` in both parsers, include it in bounded summary accounting, and scan author ∪ committer in both range and history. The end-to-end oracle was right-reason RED, then GREEN; four incomplete mutations independently kill range union, history union, streaming retention and batch retention. Author evidence `bcf11925…` reports 100 focused and 194 combined tests. Commit/push remains prohibited until chair replay plus a separate fresh contained Opus exact-tree review is CLEAN; native cybersecurity review is not retried under the standing human directive. |

Style policy (F-025, round 7; clarified by the human 2026-07-14): terse output
remains default for inter-agent, mechanical and status traffic; human-facing
explanatory output is domain-appropriate. The `$caveman` skill itself loads only
on explicit request.

**Spec-amendment prerequisites** (reconciled with
`SPEC05-APPLICABILITY.md`): D-023 and Specs 01 v0.37, 03 v1.3, 04 v1.32 and 05
v1.14 satisfy D-004's authority-profile prerequisite while keeping certifying
work read-only and the generic write profile inert pending exact containment.
D-011 risk/oracle-adjusted review still conflicts with Spec 05's mandated four
certifying review slots and requires a later Spec 05 amendment; the four-slot
profile remains binding for Spec 05 deliveries until then.

## Rejected or deferred

| Alternative | Decision | Reason |
|---|---|---|
| Single `harness.manifest.yaml` as source of truth (was D-006) | Reject (2026-07-13, round 4) | Couples unrelated change cadences into one high-blast-radius registry; per-domain owners + drift checks achieve the same guarantee. `proposals/harness.manifest.yaml` retained as a rejected illustration only. |
| Rewrite Fabric as microservices | Reject now | Distributed transactions and operations exceed demonstrated need |
| Replace Fabric with provider-native coordination | Reject | Loses neutral authority/evidence/reconciliation |
| Make MCP the scheduler/event bus/process supervisor | Reject | Wrong centre of gravity for local durable control |
| Add many persona/team skills | Reject | Increases routing/context competition; team composition should be data |
| Require GPT, Claude, Gemini and Grok on every change | Reject | High cost and correlated noise; use marginal expected value |
| Archive all state forever | Reject | Unsustainable; use retention classes and legal holds |
| Never delete compatibility code | Reject | Contradicts pre-release status and maintainability |
| Let model sessions hold release credentials | Reject | External effects require separate executor |
| Rely on hooks as security enforcement | Reject | Hooks are useful telemetry/policy assists, not sole hard boundary |
| Move all Python to TypeScript | Reject | No outcome benefit demonstrated |
| Introduce Nx/Turbo immediately | Defer | Root workspace likely sufficient initially |
| Support Windows implicitly | Reject | Declare unsupported/experimental until IPC/install/permissions are tested |
| Adopt an external orchestration framework wholesale | Reject | Importing a second lifecycle would undermine the repository's strengths |
